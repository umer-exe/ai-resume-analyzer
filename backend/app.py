from flask import Flask

app = Flask(__name__)

# Placeholder: configure extensions (CORS, env loading) later.
# Placeholder: add routes (e.g., health check, analyze) later.

if __name__ == "__main__":
    # Placeholder: replace with config-based host/port.
    app.run(debug=True)
